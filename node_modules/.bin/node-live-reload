#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-live-reload/bin/node-live-reload" "$@"
  ret=$?
else 
  node  "$basedir/../node-live-reload/bin/node-live-reload" "$@"
  ret=$?
fi
exit $ret
